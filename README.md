# 단축 URL 서비스

* **핵심기능:**
  * 긴 원본 URL을 입력하면 짧은 형태의 URL(예: `localhost:8080/aB3x9`) 발급
  * 짧은 URL로 GET 요청 시 원본 URL로 리다이렉트(HTTP 301/302) 처리
  * 해당 URL이 몇 번 클릭되었는지 조회수 카운팅

* **학습 포인트:**
  * Base62 인코딩 로직 구현 (또는 Hash + DB 시퀀스 활용)
  * HTTP 응답 상태 코드에 대한 이해
  * (여유가 된다면) Redis를 활용한 캐싱 도입 
